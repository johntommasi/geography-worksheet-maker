---
name: geography-worksheet-maker
description: "按本地规范、教材和固定 LaTeX 模板制作或修改高中地理学案、限时练，并交付经过版式核验的 PDF。"
---

# 高中地理学案制作

## 适用范围

用于本工作区的高中地理 **学案**、**限时练** 制作与修改。成品以固定 LaTeX 模板生成的 PDF 为准，不把它当作普通文档排版任务。

## 第一步：先给出制作前清单（必须执行）

收到制作或修改请求后，先阅读[制作前清单](references/制作前清单.md)，结合用户已给出的信息完整预填，并将清单发给用户确认。

- 此步骤只确认制作范围，不开始编写题目、抽取教材图片、创建或修改 `.tex`、编译 PDF。
- 未明确的信息写“待确认”或“按默认”，不得擅自补充会影响教学设计的选择。
- 用户明确回复“确认”“按清单执行”或给出补充后的最终要求后，才进入后续制作。
- 即使需求看似完整，也必须先展示这一清单；用户要求“直接执行”可视为对已展示清单的确认。

## 确认后：读取资料与规范

1. 完整读取 `要求/` 中与任务有关的文件，作为本地最高优先级规范。
2. 读取用户提供的参考资料、同类范例和 `assets/` 中对应的地理课本内容，再开始命题或写讲解。
3. 查阅 `工具脚本/latex/固定模板/` 的固定模板，只在题目内容与必要字段上做课次化填充。
4. 若用户要求“近五年考情”，检索近五年权威资料；不得编造考情结论。

路径、默认信息和版式细则见[工作区约定](references/workspace-conventions.md)。

## 成品类型与内容边界

### 学案

- 使用 `工具脚本/latex/固定模板/学案模板.tex` 与 `ruwenworksheet.sty`。
- 保持 A3 横向、双页、双栏、页眉页脚与末尾参考答案的固定结构。
- 默认：编辑 `陈浩然`，校对 `史文静`；除非用户另有要求。
- 正文为宋体、小四；小节标题沿用模板的红色标题样式。
- 内容应围绕“学习目标—核心知识/探究—随堂练习—参考答案”展开。随堂练习以选择题为主，可搭配少量填空或简答；不得在练习后堆砌无关内容。

### 限时练

- 使用 `工具脚本/latex/固定模板/限时练模板.tex` 与 `ruwenworksheet.sty`。
- 保持标题、限时与学生信息栏、A3 横向双页及答案区的固定样式。
- 不得未经说明将限时练改成学案，或反向替换。

## 图片与读图题流程

1. 优先从 `assets/` 的对应教材中提取清晰、与本课直接相关的插图。
2. 教材没有合适插图时，才检索权威或许可清楚的网络图片，并记录来源。
3. 前两者均不可用时，再绘制简洁、标注规范的示意图。
4. 每张图必须服务于知识理解或读图设问，配图题注；避免装饰性图片。

## 内容与答案要求

1. 题干、术语、图例和答案均以教材及规范为依据，题目难度符合高中阶段。
2. 题型和考查角度要有区分，避免重复设问、答案规律明显或表述含混。
3. 参考答案必须置于学案末尾，简洁、可核对，并始终留在第二页版心内。

## LaTeX 制作与输出

1. 可复用脚本只存入 `工具脚本/` 下有名称的子文件夹，绝不散落在工作区根目录。
2. 成品 `.tex` 默认保存到 `工具脚本/latex/成品源文件/`；用户指定其他位置时从其要求。
3. 优先调用 `工具脚本/latex/固定模板/编译模板.ps1` 或本机 XeLaTeX 编译。
4. 需要安装程序、宏包或字体时，必须先说明并取得用户同意。
5. PDF 输出到用户指定路径；未指定时遵循工作区约定。

## 强制版式核验

每次编译或调整版式后，都要：

1. 确认 PDF 恰为 2 页、A3 横向。
2. 渲染并查看两页全貌及页面底部。
3. 核对栏宽、页眉页脚、正文宋体小四、图像清晰度、题号连续性与答案是否越界。
4. 有空白时优先补足有价值的内容或在模板允许范围内调整间距；有溢出时先精炼、重排内容，不牺牲可读性或增加第三页。
5. 修正后必须重新编译并复查；只清理本次产生的临时文件，不动用户原有文件。

## 交付说明

最终答复先给出成品 PDF 路径，并简要说明页数、版式核验结果及需要用户确认的限制。保持简洁。
